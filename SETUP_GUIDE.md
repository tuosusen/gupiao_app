# セットアップガイド（クイックスタート）

## 最短セットアップ手順

### 1. Pythonパッケージのインストール

```bash
cd d:\gupiao_app
pip install -r requirements.txt
```

### 2. MySQLの準備

```bash
# MySQLサービスを起動（管理者権限で実行）
net start MySQL
```

### 3. データベース作成

```bash
# MySQLにログイン
mysql -u root -p

# SQLファイルを実行
source d:/gupiao_app/database/schema.sql

# または
quit
mysql -u root -p < d:\gupiao_app\database\schema.sql
```

### 4. 環境変数の設定（重要！）

**PowerShellの場合:**

```powershell
$env:MYSQL_HOST="localhost"
$env:MYSQL_PORT="3306"
$env:MYSQL_USER="root"
$env:MYSQL_PASSWORD="あなたのMySQLパスワード"
$env:MYSQL_DATABASE="stock_analysis"
```

**コマンドプロンプトの場合:**

```cmd
set MYSQL_HOST=localhost
set MYSQL_PORT=3306
set MYSQL_USER=root
set MYSQL_PASSWORD=あなたのMySQLパスワード
set MYSQL_DATABASE=stock_analysis
```

**永続的な設定（推奨）:**

1. Windowsキー + R → `sysdm.cpl` を実行
2. 「詳細設定」タブ → 「環境変数」
3. 以下を追加:
   - `MYSQL_HOST` = `localhost`
   - `MYSQL_PORT` = `3306`
   - `MYSQL_USER` = `root`
   - `MYSQL_PASSWORD` = `あなたのMySQLパスワード`
   - `MYSQL_DATABASE` = `stock_analysis`

### 5. アプリケーション起動

```bash
streamlit run stock_analysis_app.py
```

自動的にブラウザが開きます（http://localhost:8501）

### 6. 初回データ登録

アプリが起動したら：

1. **サイドバーで「データ更新」ページを選択**
2. **「データ更新」タブを開く**
3. **「接続テスト実行」ボタンをクリックして接続確認**
4. **「プライム市場全銘柄を更新」ボタンをクリック**
5. **並列処理数: 5 を選択**
6. **「更新開始」をクリック**

⏰ **初回更新には1-2時間かかります。コーヒーでも飲みながらお待ちください！**

### 7. スクリーニング実行

データ更新が完了したら：

1. **サイドバーで「銘柄スクリーニング」を選択**
2. **「データベースから検索（推奨）」にチェック**
3. **サイドバーで条件を設定:**
   - 最低配当利回り: 3.5%
   - 最大PER: 15倍
   - 平均配当利回り: 3.5%
4. **「スクリーニング実行」ボタンをクリック**
5. **結果が即座に表示されます！**

## トラブルシューティング

### エラー: データベース接続エラー

```
❌ データベース接続エラー
```

**チェックリスト:**
- [ ] MySQLサービスが起動していますか？
  ```bash
  # サービスの確認
  sc query MySQL

  # 起動していない場合
  net start MySQL
  ```
- [ ] 環境変数が設定されていますか？
  ```bash
  # PowerShellで確認
  echo $env:MYSQL_PASSWORD

  # コマンドプロンプトで確認
  echo %MYSQL_PASSWORD%
  ```
- [ ] パスワードが正しいですか？
  ```bash
  # 手動でログインテスト
  mysql -u root -p
  ```

### エラー: モジュールが見つからない

```
ModuleNotFoundError: No module named 'xxx'
```

**解決方法:**
```bash
# すべてのパッケージを再インストール
pip install -r requirements.txt --upgrade
```

### エラー: 銘柄リストの取得に失敗

```
❌ 銘柄リストの取得に失敗しました
```

**解決方法:**
- インターネット接続を確認
- しばらく待ってから再試行（JPXサイトが混雑している可能性）

### データ更新が遅い

**最適化方法:**
- 並列処理数を増やす（5 → 8）
  - ただし、10以上にするとyfinanceのレート制限に達する可能性あり
- 差分更新を使用（古いデータのみ更新）

## よくある質問（FAQ）

### Q: データはどのくらいの頻度で更新すべきですか？

**A:** 推奨スケジュール:
- 毎日: 差分更新（24時間以上経過した銘柄）
- 毎週末: 全銘柄更新
- 決算シーズン後: 必ず全銘柄更新

### Q: データベースの容量はどのくらいですか？

**A:** 約500-600MB（プライム市場全銘柄、5年分のデータ）

### Q: リアルタイムスクリーニングとデータベーススクリーニングの違いは？

**A:**
- **データベーススクリーニング（推奨）:**
  - 速度: 数秒
  - データ: 事前に更新されたデータ
  - 使用シーン: 日常的なスクリーニング

- **リアルタイムスクリーニング:**
  - 速度: 15-30分
  - データ: yfinanceから最新データを取得
  - 使用シーン: 最新データが必要な場合

### Q: 環境変数を設定し忘れました

**A:** アプリは起動しますが、データベースに接続できません。
- データ更新画面の「設定確認」タブで環境変数の状態を確認できます
- 環境変数を設定後、アプリを再起動してください

### Q: エラーメッセージが日本語で表示されない

**A:** ターミナルのエンコーディング設定を確認してください:
```bash
# PowerShellの場合
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

## 次のステップ

1. スクリーニング条件をカスタマイズ
2. 気になる銘柄を個別分析
3. CSVでエクスポートして保存
4. 定期的にデータを更新

## コマンド早見表

```bash
# アプリ起動
streamlit run stock_analysis_app.py

# MySQL起動（管理者権限）
net start MySQL

# MySQL停止
net stop MySQL

# データベース確認
mysql -u root -p stock_analysis

# パッケージ更新
pip install -r requirements.txt --upgrade

# 環境変数確認（PowerShell）
Get-ChildItem Env: | Where-Object {$_.Name -like "MYSQL*"}

# 環境変数確認（コマンドプロンプト）
set | findstr MYSQL
```

## サポートが必要な場合

1. README.mdの詳細ドキュメントを確認
2. データ更新画面の「設定確認」タブで環境をチェック
3. MySQLのエラーログを確認:
   - Windows: `C:\ProgramData\MySQL\MySQL Server X.X\Data\*.err`

---

**準備ができたら、さあスクリーニングを始めましょう！** 🚀
